<h1>Tacos!</h1>

<h2>Tacos that need to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Tacos you have already ate!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Taco Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Did you eat it already?</label><br>
    <input type="radio" name="devoured" value="1" checked> Yum!<br>
    <input type="radio" name="devoured" value="0"> Not yet!
  </div>

  <button type="submit">Add Taco</button>
</form>